import { IState } from '../../../store/rootReducer';
import { IReportTemplateWithDataViewModel } from '.';

export const reportTemplateViewModel = (state: IState): IReportTemplateWithDataViewModel => {
  return {}
};
