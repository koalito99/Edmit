import { IState } from '../../../store/rootReducer';
import { IHybridTemplateWithDataViewModel } from '.';

export const hybridTemplateViewModel = (state: IState): IHybridTemplateWithDataViewModel => {
  return {}
};
