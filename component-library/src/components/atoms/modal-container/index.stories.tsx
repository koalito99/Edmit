import * as React from 'react';
import { storiesOf } from '@storybook/react';
import ModalContainer from '.';

storiesOf('atoms/Modal Container', module).add('Default', () => (
    <div>

    </div>
));