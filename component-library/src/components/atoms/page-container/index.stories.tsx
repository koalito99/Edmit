import * as React from 'react';
import { storiesOf } from '@storybook/react';
import PageContainer from '.';

storiesOf('atoms/Page Container', module).add('Default', () => (
    <div>

    </div>
));