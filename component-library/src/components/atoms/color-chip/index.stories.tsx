import * as React from 'react';
import { storiesOf } from '@storybook/react';
import { ColorChip } from '.';

//Not sure what to display for this.

storiesOf('atoms/Color Chip', module).add('Default', () => (
    <div>

    </div>
));